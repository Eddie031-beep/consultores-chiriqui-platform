<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # No redirigir archivos ni directorios existentes
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    
    # Redirigir todo lo demás a index.php
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>